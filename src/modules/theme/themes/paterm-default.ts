import type { Theme } from "../types";

export const patermDefault: Theme = {
  id: "paterm-default",
  name: "Paterm Default",
  description: "The default Paterm look — clean glass over neutral surfaces.",
  editorTheme: { dark: "atomone", light: "atomone" },
  variants: {
    light: {},
    dark: {},
  },
};
